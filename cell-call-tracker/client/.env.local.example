# Example local env for auth and deployment
# Replace the placeholders with your real values or set these in Vercel dashboard.

DISCORD_CLIENT_ID=your_discord_client_id
DISCORD_CLIENT_SECRET=your_discord_client_secret
# Base URL of your deployment (e.g. https://your-project.vercel.app). Leave empty for relative.
BASE_URL=

# JWT signing secret (long random string)
AUTH_JWT_SECRET=change_this_to_a_random_secret

# Comma-separated Discord numeric IDs for the four editors (admin accounts)
# NOTE: For temporary preview testing, remove your own Discord ID from the Vercel
# `EDITOR_IDS` environment value so your session is treated as a non-admin. Do
# not forget to restore it after testing.
EDITOR_IDS=328647202975252481,117882062626750466,228327463690698754,158798337733033993
# Optional: temporarily exclude specific editor IDs (comma-separated). Useful for
# preview testing: set this to your Discord ID to have the server treat your
# session as non-admin without changing the main EDITOR_IDS list.
EDITOR_EXCLUDE_IDS=
